<VirtuallyRetro-File>
<source>_start:
	sys.ignorelocal
	
	lda #0.01
	out sys.performance

	lda "test.txt"
	out file.io
	
mainloop:
	lda #filebuffer
	in file.io

	mb.get.count #0 ;This checks there was no more file data
	cmp #5
	jz exit
		
	lda #filebuffer
	out text.string
	
	jp mainloop
	
exit:
	brk

;variables
filebuffer:
	mb 5

;Resources
resource.link "test.txt"</source>
<mem>1024</mem>
<row>13</row>
<col>3</col>
<hash></hash>
<scroll>0</scroll>
<autorun>false</autorun>
<exmode>false</exmode>
<fullscreen>false</fullscreen>
